Milestone 1 skeleton: repo scaffold, config schema v1, CLI shell with stub commands,
UTF-8/path/exit-code/error utilities, sentinel probes (startup-light + torch-free zone),
contract tests (version sync, config round-trip, exit codes, changelog fragments),
and Win/Linux × 3.10–3.12 CI matrix.

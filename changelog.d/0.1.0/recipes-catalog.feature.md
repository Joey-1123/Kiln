V1 recipe surface — recipe catalog (`specs/recipes/catalog.json` + `src/kiln/recipes/`).
Named, validated recipes; exposed via `kiln recipe-list` and `kiln recipe-get`. A
recipe expands into a training configuration without hand-writing hyperparameters.
